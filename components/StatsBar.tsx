import { stats } from '@/lib/inspirationSeed';
export function StatsBar(){const items=[['ideas published',stats.ideasPublished],['fictional examples',stats.fictionalExamples],['bins represented',stats.binsRepresented],['articles published',stats.articlesPublished],['awards invented',stats.awardsInvented]];return <div className="grid grid-cols-2 gap-3 md:grid-cols-5">{items.map(([l,v])=><div className="panel rounded-2xl p-4 text-center" key={l}><div className="font-display text-4xl text-bin">{v}</div><div className="text-xs uppercase tracking-widest text-silver">{l}</div></div>)}</div>}
