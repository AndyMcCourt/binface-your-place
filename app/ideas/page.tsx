import type { Metadata } from 'next';import { IdeaCard } from '@/components/IdeaCard';import { ideas } from '@/lib/ideas';
export const metadata:Metadata={title:'Ideas',description:'Count Binface-supporting household decoration missions for private-property displays.'};
export default function IdeasPage(){return <section className="mx-auto max-w-6xl px-4 py-12"><p className="eyebrow">Ideas hub</p><h1 className="font-display text-5xl uppercase">Missions for Count Binface-supporting households</h1><p className="mt-3 max-w-3xl text-silver">Choose a homemade mission, raid the recycling, and make your place visibly Count Binface. Start with one window, one bin, one fence or one front garden. The aim is not polish. The aim is unmistakable democratic weirdness.</p><div className="mt-8 grid gap-5 md:grid-cols-2 lg:grid-cols-3">{ideas.map(i=><IdeaCard key={i.slug} idea={i}/>)}</div></section>}
