import type { Metadata } from 'next';import { articles } from '@/lib/articles';import { ArticleCard } from '@/components/ArticleCard';export const metadata:Metadata={title:'Articles',description:'Magazine-style static articles for Binface Your Place.'};
export default function Articles(){return <section className="mx-auto max-w-6xl px-4 py-12"><p className="eyebrow">Magazine</p><h1 className="font-display text-5xl">Articles from the nonsense moonbase</h1><div className="mt-8 grid gap-5 md:grid-cols-2">{articles.map(a=><ArticleCard key={a.slug} article={a}/>)}</div></section>}
