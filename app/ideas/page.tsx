import type { Metadata } from 'next';import { IdeaCard } from '@/components/IdeaCard';import { ideas } from '@/lib/ideas';export const metadata:Metadata={title:'Ideas',description:'Static Binface Your Place ideas for private-property parody displays.'};
export default function IdeasPage(){return <section className="mx-auto max-w-6xl px-4 py-12"><p className="eyebrow">Ideas hub</p><h1 className="font-display text-5xl">Ideas for lawful ridiculousness</h1><p className="mt-3 max-w-3xl text-silver">Article-style suggestions only. No downloadable kits, no submissions, no targeting, no public-property sticker album.</p><div className="mt-8 grid gap-5 md:grid-cols-2 lg:grid-cols-3">{ideas.map(i=><IdeaCard key={i.slug} idea={i}/>)}</div></section>}
